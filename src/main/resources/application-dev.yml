spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: always

loki:
  endpoint: ${LOKI_ENDPOINT}

frontend:
  base-url: ${FRONTEND_BASE_URL}